# Secrets — never commit. Copy secrets.yaml.example to secrets.yaml and fill in.
secrets.yaml
secrets_*.yaml
# Any hand- or editor-made copy of secrets.yaml (secrets.yaml.bak) matches
# neither pattern above, so cover the backup suffix too.
*.bak

# ESPHome build artifacts
.esphome/
*.bin
*.elf

# Editor / OS cruft
.DS_Store
*.swp
